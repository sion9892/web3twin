# ============================================
# Vercel Production 환경 변수 설정 가이드
# ============================================
# Vercel 대시보드에서 Settings > Environment Variables에 다음 변수들을 등록하세요.
# 프로덕션 환경에서는 반드시 Production 환경에 설정해야 합니다.

# ============================================
# 클라이언트 접근 가능 (VITE_ 접두사)
# ============================================

# Neynar API Configuration
# Vercel에서: VITE_NEYNAR_API_KEY = (실제 API 키)
# 환경: Production, Preview, Development 모두 설정
VITE_NEYNAR_API_KEY=your_neynar_api_key_here

# Base URLs
# 🚨 중요: 프로덕션에서는 빈 값 또는 https://your-app.vercel.app 형식으로 설정
# Vercel Production에서: VITE_API_BASE_URL = (빈 값 또는 https://web3twin.vercel.app)
# 개발 환경에서는 http://localhost:3000 또는 빈 값 (상대 경로 사용)
# 빈 값이면 프로덕션에서 자동으로 상대 경로(/api/...)를 사용합니다
VITE_API_BASE_URL=

# Coinbase OnchainKit Configuration
# Vercel에서: VITE_COINBASE_API_KEY = (실제 API 키)
# 환경: Production, Preview, Development 모두 설정
VITE_COINBASE_API_KEY=your_coinbase_api_key_here

# WalletConnect Project ID
# Vercel에서: VITE_WALLETCONNECT_PROJECT_ID = (실제 프로젝트 ID, 32자)
# 환경: Production, Preview, Development 모두 설정
VITE_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id_here

# Base RPC URLs
# Vercel에서: VITE_BASE_RPC_URL = https://mainnet.base.org
# 환경: Production, Preview, Development 모두 설정
VITE_BASE_RPC_URL=https://mainnet.base.org

# NFT Storage API Key (if used)
# Vercel에서: VITE_NFT_STORAGE_API_KEY = (실제 API 키)
# 환경: Production, Preview, Development 모두 설정 (사용하는 경우)
VITE_NFT_STORAGE_API_KEY=your_nft_storage_api_key_here

# ============================================
# 서버/빌드 전용 (VITE_ 접두사 없음 - 클라이언트 접근 불가)
# ============================================

# Blockchain Configuration
# Vercel에서: PRIVATE_KEY = (실제 프라이빗 키)
# 환경: Production에만 설정 (주의: 민감한 정보)
# ⚠️ 절대 클라이언트 코드에서 사용하지 마세요
PRIVATE_KEY=your_private_key_here

# Pinata IPFS Configuration (서버 사이드에서만 사용)
# Vercel에서: PINATA_API_KEY = (실제 API 키)
# 환경: Production에만 설정
PINATA_API_KEY=your_pinata_api_key_here
# Vercel에서: PINATA_SECRET_KEY = (실제 시크릿 키)
# 환경: Production에만 설정
PINATA_SECRET_KEY=your_pinata_secret_key_here
# Vercel에서: PINATA_GATEWAY = https://gateway.pinata.cloud/ipfs/
# 환경: Production에만 설정
PINATA_GATEWAY=https://gateway.pinata.cloud/ipfs/

# RPC URLs (서버 사이드)
# Vercel에서: BASE_SEPOLIA_RPC_URL = https://sepolia.base.org
# 환경: Production에만 설정 (필요한 경우)
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
# Vercel에서: BASE_RPC_URL = https://mainnet.base.org
# 환경: Production에만 설정 (필요한 경우)
BASE_RPC_URL=https://mainnet.base.org
# Vercel에서: SEPOLIA_RPC_URL = (Infura URL)
# 환경: Production에만 설정 (필요한 경우)
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/your_infura_key

# ============================================
# 개발 전용 (로컬 개발 환경에서만 사용)
# ============================================

# Development mode (use direct API calls instead of proxy)
USE_DIRECT_API=true

# Contract Addresses (will be auto-generated)
CONTRACT_ADDRESS_HARDHAT=0x5FbDB2315678afecb367f032d93F642f64180aa3
CONTRACT_ADDRESS_BASE_SEPOLIA=0x...
CONTRACT_ADDRESS_BASE=0x...

# ============================================
# Vercel 환경 변수 설정 체크리스트
# ============================================
# ✅ Production 환경에 설정해야 할 변수:
#   1. VITE_NEYNAR_API_KEY
#   2. VITE_API_BASE_URL (빈 값 또는 프로덕션 URL)
#   3. VITE_COINBASE_API_KEY
#   4. VITE_WALLETCONNECT_PROJECT_ID
#   5. VITE_BASE_RPC_URL
#   6. PRIVATE_KEY (서버 사이드만)
#   7. PINATA_API_KEY (서버 사이드만)
#   8. PINATA_SECRET_KEY (서버 사이드만)
#   9. PINATA_GATEWAY (서버 사이드만)
#
# ⚠️ 중요: VITE_API_BASE_URL을 빈 값으로 두면 
#          프로덕션에서 자동으로 상대 경로(/api/...)를 사용합니다.
#          이렇게 하면 localhost 에러가 발생하지 않습니다.
# ============================================
